package game;

import java.util.ArrayList;
import java.util.List;

public class Test {
    public static void main(String[] args) {
        /**
        SosBoard board = new SosBoard(5);
        int points = 0;
        try {
            points += board.move('O', 1, 1);
            points += board.move('O', 2, 1);
            points += board.move('O', 3, 1);
            points += board.move('O', 1, 2);
            points += board.move('O', 3, 2);
            points += board.move('O', 1, 3);
            points += board.move('O', 2, 3);
            points += board.move('O', 3, 3);
            points += board.move('S', 0, 0);
            points += board.move('S', 0, 2);
            points += board.move('S', 0, 4);
            points += board.move('S', 2, 0);
            points += board.move('S', 2, 4);
            points += board.move('S', 4, 0);
            points += board.move('S', 4, 4);
            points += board.move('S', 4, 2);
            points += board.move('S', 2, 2);
        }
        catch (Exception e) {
            System.out.print(e.getStackTrace());
        }
        System.out.print(board.toString());
        System.out.println("" + points);


        List<String> players = new ArrayList<String>();
        players.add("one");
        players.add("two");
        players.add("three");
        Game game = new Game(5, players);
        game.move('O', 1, 1);
        game.move('O', 2, 1);
        game.move('O', 3, 1);
        game.move('O', 1, 2);
        game.move('O', 3, 2);
        game.move('O', 1, 3);
        game.move('O', 2, 3);
        game.move('O', 3, 3);
        game.move('S', 0, 0);
        game.move('S', 0, 2);
        game.move('S', 0, 4);
        game.move('S', 2, 0);
        game.move('S', 2, 4);
        game.move('S', 4, 0);
        game.move('S', 4, 4);
        game.move('S', 4, 2);
        game.move('S', 2, 2);
        System.out.print(game.getBoard());
         */
    }
}
